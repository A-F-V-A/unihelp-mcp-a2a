/**
 * Entorno de DESARROLLO (`nx serve web`). Apunta al backend real igual que
 * produccion: la unica diferencia es que no se optimiza el bundle.
 */
export const environment = {
  production: false,
};
